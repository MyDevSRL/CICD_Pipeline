name: Continuous Integration And Deployement

on:
  push:
    branches: main


jobs: 
  build:
    runs-on: windows-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '6.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build app
        run: dotnet build -c Release --no-restore

      - name:  Run automated tests
        run: dotnet test -c Release --no-build

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v1.1

      - name: Deploy  
          uses: appleboy/scp-action@master
            env:
              HOST:192.168.237.58
              USERNAME: web\giulio.bacchilega
              PASSWORD: "&Imperiale1121"
              PORT: 22
            with:
              source: "*"
              target: "C:\inetpub\wwwroot\ProvaCICD_Giulio"
        # uses: garygrossgarten/github-action-ssh@release
        # with:
        #   command: sudo systemctl restart project-name
        #   host: ${{ secrets.REMOTE_HOST }}
        #   username: ${{ secrets.REMOTE_USER }}
        #   privateKey: ${{ secrets.REMOTE_SSH_KEY }}
        #run: dotnet publish TestPipeline -c Release -o \\192.168.237.58\c$\inetpub\wwwroot\ProvaCICD_Giulio --self-contained false
        #run: msbuild TestPipeline /p:DeployOnBuild=true /p:PublishProfile=test
        # uses: SamKirkland/FTP-Deploy-Action@4.0.0
        # with:
        #   server: 192.168.237.58
        #   username: "web\\giulio.bacchilega"
        #   password: "&Imperiale1121"
        #   server-dir: "C:/inetpub/wwwroot/ProvaCICD_Giulio/"