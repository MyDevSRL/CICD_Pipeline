name: Continuous Integration And Deployement

on:
  push:
    branches: main


jobs: 
  build:
    runs-on: windows-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: '6.0.x'

      - name: Restore dependencies
        run: dotnet restore

      - name: Build app
        run: dotnet build -c Release --no-restore

      - name:  Run automated tests
        run: dotnet test -c Release --no-build

      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }} 
          known_hosts: 'just-a-placeholder-so-we-dont-get-errors'
      
      - name: Dotnet Publish
        run: dotnet publish TestPipeline -c Release -o deploy
      
      - name: Copy via ssh
        uses: garygrossgarten/github-action-scp@v0.5.3
        with:
          local: C:\Users\giulio.bacchilega.WEB\Desktop\GitHub_CICD_Pipeline\CICD_Pipeline\deploy
          remote: C:\inetpub\wwwroot\ProvaCICD_Giulio
          host: 192.168.237.58
          username: web\giulio.bacchilega
          privateKey: ${{ secrets.SSH_PRIVATE_KEY }}
      # - name: Deploy with rsync
      #   run: rsync -avz ./dist/ giulio.bacchilega@192.168.237.58:/inetpub/wwwroot/ProvaCICD_Giulio/
      # - name: Add msbuild to PATH
      #   uses: microsoft/setup-msbuild@v1.1

      # - name: Deploy  
      #   run: dotnet publish TestPipeline -c Release -o \\192.168.237.58\c$\inetpub\wwwroot\ProvaCICD_Giulio --self-contained false
        #run: msbuild TestPipeline /p:DeployOnBuild=true /p:PublishProfile=test
        # uses: SamKirkland/FTP-Deploy-Action@4.0.0
        # with:
        #   server: 192.168.237.58
        #   username: "web\\giulio.bacchilega"
        #   password: "&Imperiale1121"
        #   server-dir: "C:/inetpub/wwwroot/ProvaCICD_Giulio/"